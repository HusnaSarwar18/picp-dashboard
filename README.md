# PICP Digital Verification Dashboard

This repository contains the frontend for the Punjab Inclusive Cities Program (PICP) digital verification and monitoring dashboard.

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
